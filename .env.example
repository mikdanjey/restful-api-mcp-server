# MCP RESTful API Server for RESTful API operations and JSON Server
# Copy this file to .env and update the values

# Required: Base URL of the REST API
# Examples:
# - JSONPlaceholder (demo API): https://jsonplaceholder.typicode.com
# - Local JSON Server: http://localhost:3000
# - Your API: https://api.example.com
API_BASE_URL=https://jsonplaceholder.typicode.com

# Required: Authentication type (basic, token, or none)
API_AUTH_TYPE=none

# Optional: Bearer token for token authentication (required when API_AUTH_TYPE=token)
# API_AUTH_TOKEN=your-bearer-token-here

# Optional: Basic authentication credentials (required when API_AUTH_TYPE=basic)
# API_BASIC_AUTH_USERNAME=your-username
# API_BASIC_AUTH_PASSWORD=your-password

# Optional: Enable debug logging
# DEBUG=true

# Optional: Node.js environment
# NODE_ENV=development